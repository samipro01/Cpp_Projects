#include <iostream>
#include <string>

int main()
{
    // int(integer)
    int momo_number = 1230222;
    std::cout << momo_number << std::endl;

    // char(character)
    char currency = '$';
    std::cout << currency << std::endl;

    // string(text, sentences and symbols)
    std::string date = "24th January 2026";
    std::cout << date << std::endl;

    //double balance
    double balance = 45.66;
    std::cout << balance << std::endl;

    // Boolean(True or false);
    bool prompt;
    prompt = false;
    std::cout << prompt << std::endl;

    return 0;
}